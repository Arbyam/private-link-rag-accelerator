"""Shared test utilities and fixtures (T046).

Duplicated verbatim under ``apps/ingest/tests/_shared`` because the two test
suites are independent ``pyproject.toml`` projects with separate ``sys.path``
roots; duplication is preferred over a top-level path hack.
"""
